What I can do:

Use foreground terminal commands only, so you can watch output live.
Avoid background clone/build jobs unless you explicitly ask for them.
Call out when a command is long-running before I start it.
Summarize the important result after each visible run.